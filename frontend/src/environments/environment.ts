// src/environments/environment.ts
export const environment = {
    production: false,
    apiUrl: '/api',   // Proxy forwards to localhost:8080
    wsUrl:  'ws://localhost:4200/ws',      // Proxy forwards to localhost:8080
  };